# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SNSDemoApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

pod 'Firebase/Analytics'
pod 'Firebase/Auth'
pod 'Firebase/Firestore'
pod 'Alamofire', '~> 5.1'
pod 'XCGLogger', '~> 7.0.1'

  # Pods for SNSDemoApp

  target 'SNSDemoAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SNSDemoAppUITests' do
    # Pods for testing
  end

end
